<h1><%= t('users.list', resource: User.model_name.human) %></h1>

<ul>
  <% @users.each do |user| %>
  <li><%= link_to user.email, user %></li>
  <% end %>
</ul>

<%= paginate @users %>

<%= link_to t('views.common.back', name: i18n_pluralize(Book.model_name.human.downcase)), books_path %>
